---
---

<hr>
<div class="maintenance">
    <div class="bg-gradient" style="background-image: linear-gradient(to top, rgba(255, 0, 0, 0.075), rgba(255, 0, 0, 0.05),rgba(0, 0, 0, 0.0), rgba(0, 0, 0, 0));"></div>

    <span class="medium">WOOPS! THE GAME IS CURRENTLY UNDER</span>
    <span class="big">MAINTENANCE</span>
    <span class="small"><b>Servers are closed. Please wait for further updates.</b></span>

    <div class="stripes-scrolling left"></div>
    <div class="stripes-scrolling right"></div>
</div>
<hr>

<style>
    div.maintenance {
        display: flex;
        align-items: center;
        justify-content: space-around;
        flex-direction: column;

        width: 100%;
        height: 200px;

        border: 1px solid #fe000a;
        border-radius: 4px;

        overflow: hidden;
        position: relative;

        background-color: #100;
        background-image: linear-gradient(to right,#400 1px,transparent 1px),linear-gradient(to bottom, #400 1px,transparent 1px);
        background-size: 32px 32px;
        background-position: center;
        background-position-y: 0px;

        animation: gridup linear 2s infinite;
    }

    div.stripes-scrolling {
        position: absolute;
        background-image: url(/anisa/images/banner/maintenance_lines.png);
        background-position: 0 0;
        background-size: 32px 32px;

        width: 32px;
        height: 100%;

        animation: goup linear 0.5s infinite;

        box-shadow: black 0 0 4px, black 0 0 6px, black 0 0 8px;
    }

    div.stripes-scrolling.left {
        left: 0;
        border-right: 1px #fe000a solid;
    }

    div.stripes-scrolling.right {
        right: 0;
        border-left: 1px #fe000a solid;
    }

    span.small {
        display: block;
        font-size: 16px;
        text-shadow: black 0 0 4px, black 0 0 4px;
    }

    span.medium {
        display: block;
        font-size: 24px;
        font-family: 'Abolition', sans-serif;
        text-shadow: black 0 0 4px, black 0 0 4px;
    }

    span.big {
        display: block;
        font-size: 128px;
        font-family: 'American Captain', monospace;
        text-shadow: black 0 0 4px, black 0 0 4px;
    }

    @keyframes goup {
        0% {
            background-position-y: 0px;
        }

        100% {
            background-position-y: -32px;
        }
    } 

    @keyframes gridup {
        0% {
            background-position-y: 0px;
        }

        100% {
            background-position-y: -32px;
        }
    }
</style>