---
---

<div class="advise-search" id="advise-search">
    <div class="advise-search-wrapper">
        <input type="text" placeholder="🔍 Search for questions, answers, or lines..." id="advise-searchbar" />
    </div>
</div>

<style>
    div.advise-search {
        display: flex;
        margin: auto;
        width: 100%;
        align-items: center;
        justify-content: center;
        margin: 16px 0;
    }

    div.advise-search-wrapper {
        display: flex;
        width: max-content;
        width: 100%;
        align-content: center;
        justify-content: center;
        padding: 8px;
        background-image: radial-gradient( at top right, var(--darkerer), var(--darkererer) );
        border: var(--dark) 1px solid;
        border-radius: 4px;
        font-size: 16px;
        flex-wrap: wrap;
        gap: 8px;
    }

    div.advise-search input {
        padding: 12px 16px;
        outline: none;
        background-color: var(--darker);
        border: none;
        color: var(--lightererest);
        font-family: 'Pretendard', sans-serif;
        border-radius: 4px;
        font-size: 16px;
        width: 100%;
    }
</style>